#include <stdio.h>
#include <stdlib.h>

int main()
{
    /* arithmetic operators
    + plus
    * multiply
    / division
    % mod
    ++ increment
    -- decrement

    */

    /* relational operators
    == equal
    != not equal
    > greater
    < lesser
    >= greater or equal
    <= lesser or equal

    */

    /* logical operators
    && and
    || or
    ! not
    */
    /* beta wise operator
    & and
    | or
    ex : 10 and/or 20
    10: 0000 1010
    20: 0001 0100
    --------------
    and: 0000 0000
    or:  0001 1110

    int x=10, y=20, z, k;
    z=x & y;
    k=x | y;
    printf("and: %d \n",z);
    printf("or: %d \n",k);
    */

    return 0;
}
