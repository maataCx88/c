#include <stdio.h>
#include <stdlib.h>
#include <string.h>
void sort(char *s){
int c, d=0, l;



}

int main()
{
    /* strings */
   /* strcpy Copies the C string pointed by source into the array pointed by destination
    strcat concatination
    strlen length
    strcmp return 0 if 2 string variables same
    strchr return to pointer to first occurance of character
    strstr return to first occurance of string in string variable
    \0 means this is the end of our string to save memory*/

   /* char display[12]={'H','e','l','l','o',' ','w','o','r','l','d','\0'};
    printf("This is first project in C is %s \n",display);

    /* string functions */
    /*char strl[]="Firstname";
    char str2[]="Lastname";
    char str3[]=" ";
    int len;
    /* copy first name and lastname it copies str2 to str1*/
   // strcpy(strl,str2);
   //  printf("The result of strcpy is : %s \n",strl);

   // Firstnamelastname
  //  strcat(strl,str2);
  //  printf("The result of strcat is : %s \n",strl);
    //get length of string
 /* len=strlen(strl);
  printf("The result of strlen : %d \n",len);*/


  /* reverse string */
   /* char strr[200];
    printf("Please insert your name : ");
    gets(strr);
    strrev(strr);
    printf("the reverse string is : %s",strr);
    return 0;

    /* get length of string */
    char strr1[100];
    printf("Please insert a word: ");
    gets(strr1);
    int x=strlen(strr1);
    printf("length of the word %s is : %d",strr1,x);
}
